<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rivaldofez.moviers">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Moviers"
        tools:ignore="AllowBackup">
        <activity android:name=".ui.webview.WebViewActivity"/>
        <activity android:name=".ui.detail.movie.DetailMovie" />
        <activity android:name=".ui.detail.tvshow.DetailTvShow" />
        <activity android:name=".ui.home.HomeActivity"
            android:theme="@style/Theme.Moviers.NoToolbar"/>
        <activity
            android:name=".ui.splash.SplashActivity"
            android:theme="@style/Theme.Moviers.NoToolbar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>